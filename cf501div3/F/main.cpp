#include <iostream>
#include <bits/stdc++.h>
using namespace std;
stack<char>ss;
char s[1000];
int main()
{
    int n;
    cin>>n;
    scanf("%s",s);
    int l=0,r=0;
    for(int i=0;i<n;i++)
    {
        if(!ss.empty())
        {
            if(s[i]==')')
        }
    }
    return 0;
}
