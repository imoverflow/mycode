#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<int>v;
bool vis[505];
int main()
{
    int n,k;
    memset(vis,0,siz vis);
    scanf("%d%d",&n,&k);
    for(int i=1;i<=n;i++)
    {
        scanf("%d",&s[i]);
        v.push_back(s[i]);
    }
    int x=v.front();
    while(vis[x]==false)
    {

    }
    return 0;
}
