#include <stdio.h>


main()
{
    int a,b,c,I,J,K,L;
    char s[500][16];
    int st[500];
    while(1)
    {
        for(J=0;J<500;J++)
            st[J]=1;
        scanf("%d",&a);
        for(I=0;I<a;I++)
        {
            scanf("%s",s[I]);
        }
        for(K=0;K<a;K++)
        {
            for(J=K+1;J<a;J++)
                if(s[J]==s[K])
                    st[]
        }
    }
}
